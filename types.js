// types complexes : les objets et les tableaux

// tyopes primitifs

// chaine de caractères :
// guillemets :
const firstName = "Chris";
// backticks :
const city = `Paris`;
// simple guillements : 
// pour échapper un caractère, mettre un anti-slash \ => shitf + option + /
const address = '9 rue de l\'Ouie';
console.log(address);

// Nombres (pour les valeurs numériques):
let age = 11;
let amount = 12.5; // attention, utiliser le point et non la virgule
let temerature = -4;

// Booléens (vrai ou faux):
let isPresent = true;
let isAdmin = false;

// undefined => qui n'existe pas:
let x; //undefined
let y = undefined; 

console.log(y);

// null => RIEN
let email = null;
console.log(email);
